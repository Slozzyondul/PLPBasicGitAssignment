/*
// class to represent an animal
//properties
class Animal {
  String name;
  String species;

  //constructor
  Animal(this.name, this.species);

  //method to make ethe animal sound
  void makeSound() {
    print('$name (${species}) makes a sound');
  }
}

void main() {
  //creating instances (objects) of the animal class
  var cat = Animal('tom', 'cat');
  var dog = Animal('scobby', 'dog');
  var cow = Animal('mrembo', 'cow');

//accessing properties and behaviours
  print('${cat.name} is a ${cat.species}.');
  cat.makeSound();

  print('${dog.name} is a ${dog.species}.');
  dog.makeSound();

  print('${cow.name} is a ${cow.species}.');
  cow.makeSound();
}
*/
