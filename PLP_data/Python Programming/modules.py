#modules = reusable pieces of code that can be imported into a program to provide additional functionality
#module can have (functions, classes, variables, and constants that can be accessed by importing them into a Python script)
#built-in modules include math, datetime, and random, while popular third-party modules include pandas, NumPy, and matplotlib.

#module addition
def add(a,b):
    result = a + b
    return result
